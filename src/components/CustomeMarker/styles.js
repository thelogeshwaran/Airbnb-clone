import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  price: {
    borderWidth: 1,
    borderRadius: 20,
    padding: 5,
    borderColor: 'grey',
  },
});
export default styles;
