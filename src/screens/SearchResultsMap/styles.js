import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: '100%',
  },

  carousel: {
    position: 'absolute',
    bottom: 30,
  },
});
export default styles;
